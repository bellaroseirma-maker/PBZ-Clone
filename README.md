# PBZ-Clone
pbz_proxy: Local reverse proxy for PBZ used for authorized testing. Two modes: exact-domain (server_www.js ) for 1:1 rendering via hosts+TLS, and transform (server.js) for shareable/ngrok links with CSP/base rewriting. Logs requests and safely records login attempts (no secrets).
